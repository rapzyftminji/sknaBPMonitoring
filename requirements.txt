# SKNA / Vitals Analysis GUI + SKNA->BP training dependencies.
# Install into a fresh venv:
#     python3 -m venv venv
#     source venv/bin/activate
#     pip install -r requirements.txt
#
# torch is listed last on purpose: if the default wheel fails to load (no GPU,
# missing CUDA), install the CPU build instead, per src/train_worker.py's message:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu

numpy
pandas
scipy
matplotlib
PyQt5
pyqtgraph
PyWavelets
torch
